interface:
  display_name: "SketchUp Modeling"
  short_description: "Model and validate SketchUp scenes through the local OpenSU extension"
  default_prompt: "Use $sketchup-modeling to inspect the current SketchUp model, execute the requested architectural modeling in millimetres, and validate the result before finishing."
